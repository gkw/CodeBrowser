# Optional single-user development bridge to the private managed Wrapper.
# Remote origins must use HTTPS. Never commit a real actor token.
CODE_BROWSER_MANAGED_WRAPPER_URL=
CODE_BROWSER_MANAGED_ACCESS_TOKEN=
